# Health Check Findings — 2026-08-24

- Run type: Scheduled daily run
- Source commit: `a5a67f00e6eba91220871639fc55e267b611045b` (`chore(vault): daily project truth refresh`, 2026-08-24T01:11:16Z)
- CI evidence: GitHub Actions run `32678992095` for exact HEAD `a5a67f0` — conclusion: **success** (job `validate`, Vault Validation workflow)
- Open PRs at run time:
  - **PR #7** `fix(health-check): reconcile stale Vault counts/docs (2026-08-23)` — MERGEABLE, checks passing, **unmerged since 2026-08-23**
  - **PR #6** `fix(health-check): reconcile stale Vault counts/docs (2026-08-21)` — MERGEABLE, **unmerged since 2026-08-21** (superseded by PR #7)
- Recently merged PRs: #5 (2026-08-19), #4 (2026-08-19), #3 (2026-08-18)
- Work-order pointer at run time: `04 Work Orders/CURRENT_WORK_ORDER.md` — STATUS: CLOSED, LATEST_CLOSED: WO-OBSIDIAN-041, ACTIVE_WORK_ORDER: none (consistent with `Work Order Index.md`)

## Overall state
HEALTHY-WITH-FINDINGS (recurring — fixes pending merge in PR #7)

## Findings

All four findings below are **already fixed in open PR #7** (verified by inspecting the PR #7 diff). They remain present on `main` only because PR #7 has not been merged. This run deliberately does **not** re-apply the same edits on a third branch — doing so would create a duplicate/conflicting PR. The required action is an owner merge decision.

1. `README.md` line 56 — `Validate ทุก state file (ต้อง 18/18 VALID)` is stale: verification this run shows `Validation: 19/19 VALID` (19 state files in `automation/state/`, including `RX-8.yaml`)
   - severity: LOW
   - status: **fix pending in PR #7** (`ต้อง 19/19 VALID`); also fixed in PR #6
   - root cause: Rx 8 onboarded via WO-OBSIDIAN-037; quickstart count not updated on main because the fix PRs were never merged

2. `00 Dashboard/Project Dashboard.md` — claims `รายชื่อ Project Overview 18 รายการ` and the Imported Projects list omits `[[Rx 8]]`, while `01 Projects/Rx 8.md`, `automation/state/RX-8.yaml`, and the Live Project Wall row all exist on main
   - severity: LOW
   - status: **fix pending in PR #7** (count → 19, `[[Rx 8]]` added)
   - root cause: same as #1

3. `01 Projects/Project Index.md` — no row for `[[Rx 8]]` (18 rows vs 19 imported projects on main)
   - severity: LOW
   - status: **fix pending in PR #7** (Rx 8 row added with GitHub-adapter evidence)
   - root cause: same as #1

4. Stale status headers in Work Order files:
   - `04 Work Orders/WO-OBSIDIAN-004-ONBOARD-LLM-AGENTS.md` header = `Status: PLANNED` but Work Order Index + CURRENT_WORK_ORDER record WO-004 = CLOSED
   - `04 Work Orders/WO-OBSIDIAN-008-ONBOARD-UTILITY-DISBURSEMENT-APP.md` header = `Status: PLANNED — NEXT` but Work Order Index + CURRENT_WORK_ORDER record WO-008 = CLOSED
   - severity: LOW
   - status: **fix pending in PR #7** (headers → `Status: CLOSED`); not covered by PR #6
   - root cause: WO file headers not updated at close-out — same class of issue as WO-002/003 fixed on 2026-08-19

## Root cause (systemic, recurring since 2026-08-21)

The daily health-check automation opens fix PRs, but **none of the last three fix PRs (#6, #7) have been merged**. Each subsequent daily run therefore re-discovers the same staleness on `main` and opens another overlapping PR. The fix pipeline works; the merge step is the missing link. This is an owner-side process gap, not a code defect.

## Verification this run

- `python3 automation/migrate_state_v2.py --validate` → `Validation: 19/19 VALID (exit code 0)`
- `python3 scripts/render_project_wall.py --validate-all` → exit 0 (19 state files VALID)
- `python3 -m pytest tests/ -q` → **148 passed**
- Exact-head CI: run `32678992095` on `a5a67f0` → success
- Pointer consistency: CURRENT_WORK_ORDER (LATEST_CLOSED: WO-OBSIDIAN-041, ACTIVE: none) == WO-OBSIDIAN-041 file (STATUS: CLOSED) == Work Order Index — **consistent**
- Repo layout note (verified, not an issue): no `.openhands/`, `work_orders/`, or `docs/SYSTEM_INTEGRATION_READINESS.md` — the real layout uses `04 Work Orders/CURRENT_WORK_ORDER.md` as state authority (consistent with 2026-08-17/19 findings)

## Owner attention

1. **Merge decision required (blocking recurrence):** merge **PR #7** (the most complete fix — superset of PR #6) and close PR #6, or merge PR #6 plus the WO-004/008 header fixes from PR #7. Until one merges, every daily run will re-report findings 1–4.
2. Rx 8 remains `needs-verification` in both the state file and (via PR #7) the Project Index row — awaits owner verification per the WO-OBSIDIAN-037 model (not auto-fixable).
3. No active Work Order — next onboarding/planning decision belongs to the owner.
4. Consider whether the daily health-check automation should open an issue instead of a PR when an equivalent fix PR is already open, to avoid PR pile-up.

## Follow-up

- Owner merges PR #7 and closes PR #6 → findings 1–4 close on the next daily run.
- Owner creates the next Work Order per Planned Sequence when ready.

0 secrets. 0 source-repo modifications. No CI/config changes. No main-branch mutations (all changes on `fix/health-check-issues-2026-08-24`).
