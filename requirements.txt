# Runtime dependencies for scripts/render_project_wall.py
# Created by WO-OBSIDIAN-033 (Runtime Reproducibility Foundation).
# Pin ranges, not exact pins, to avoid unnecessary churn; tighten in WO-035 if needed.

PyYAML>=6.0,<7.0
jsonschema>=4.18,<5.0

# Test dependency for the WO-OBSIDIAN-035 regression suite (tests/).
# pytest is required to run `python3 -m pytest tests/` and the regression
# gate inside automation/refresh_state.py.
pytest>=7.0,<9.0
